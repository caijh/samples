server:
  port: 8080
spring:
  application:
    name: zuul-gateway
eureka:
  client:
    service-url:
      defaultZone: ${eureka_server_addresses:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
debug: true
